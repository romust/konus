package ru.ustyantsev.konus.ui.Activities.utils;

import android.support.v4.app.Fragment;

public interface FragmentReplacement {//1.1 реализуем интерфейс с методом, который будет использовать активити
    void fragmentReplacement(Fragment fragment);
}
